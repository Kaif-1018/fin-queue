# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database / ORM
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Migrations
alembic==1.13.2

# Task queue
celery[redis]==5.4.0

# Redis client
redis==5.0.7

# Settings management
pydantic-settings==2.3.4

# Auth — password hashing and JWT
# bcrypt is used directly rather than through passlib: passlib 1.7.4 reads
# bcrypt.__about__, which bcrypt 4.x removed, and raises on import.
bcrypt==4.2.0
pyjwt==2.9.0

# Pydantic EmailStr
email-validator==2.2.0

# Form parsing — OAuth2PasswordRequestForm (login) and UploadFile (ingest)
python-multipart==0.0.9

# Structured logging
structlog==24.2.0

# WebSocket support (included with uvicorn[standard])
websockets==12.0

# Data generation (seeding)
Faker==25.8.0
